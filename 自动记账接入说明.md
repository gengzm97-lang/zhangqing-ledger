# 手机自动记账接入

原账清网页、登录账号与历史账目保留。此接入为单向收集：手机 AutoAccounting 识别账单 → Supabase 接收箱 → 打开账清时合并到个人支出。

如果手机桌面版找不到新入口，请在原来安装账清的浏览器中打开 [保留账目的更新页](./update.html)，点击更新。它仅更新程序文件，不清除浏览器账目、登录信息或云端数据，成功后自动定位到连接设置。不需要卸载应用或清除网站数据。

## 手机连接

1. 手机安装官方 AutoAccounting，优先选择免 Root、本地 OCR；不要为此提供支付密码或开启云端图片识别。
2. 手机浏览器打开账清，进入「数据管理」，登录原云同步账号。
3. 在「手机自动记账连接」点击「生成连接配置」。这一步只创建上传凭据，还没有收到任何消费。
4. 到 AutoAccounting 的「数据管理 → 账单导出」，把账清的地址复制到「NAS 接收地址」，令牌复制到「NAS 令牌」，打开「同步账单到 NAS」。不需要购买 NAS。
5. 点击「立即导出」，再回到账清点「检查新账单」。先核对一次真实消费是否正确识别。

令牌仅在生成的页面内显示，刷新/退出后不保留明文。如果忘记，可在原设备旁点「重置令牌」，再更新手机中的令牌。普通轮换会保留去重身份；只有清除 AutoAccounting 数据或重装后才新建设备连接。

## 记账规则

- 只把明确的人民币、微信/支付宝普通消费候选自动加入个人支出。
- 转账、退款、还款、借款、收入、经营用途、手续费、未知金额/币种等进入待确认，不直接计入个人消费。
- 同日同金额、且支付方式相同或手记方式留空的记录会提示疑似重复。先自动记账、后导入月账单也会提示。
- 一条自动记录重复推送不会再次记入。上游修改已经入账的记录时，先提示核对，不直接改原账。
- 忽略操作随账清云同步保存；已删除的自动支出不会因再次推送而恢复。
- 日期按支付记录的北京时间显示。网页关闭时接收箱仍能保存手机推送，打开网页后检查并合并；保持打开时通常每分钟检查一次。

## 明确限制

AutoAccounting 不是微信或支付宝官方流水 API。能否识别取决于手机权限、后台限制和支付页面；本次云端部署不能证明真实支付识别已经成功。项目后台调度可能延迟，不能承诺每笔实时无遗漏。上游不会推送删除，自动导出主要覆盖当日及前日；多日离线或历史修改可能需要账单文件补账。仍建议定期用月度账单核对。

## 安全与接口

REST 边界只负责接收与设备管理，不直接修改原来的 `ledger_snapshots`。采用独立接收箱避免外部采集器覆盖整个账本。

| 接口 | 验证方式 | 用途 |
| --- | --- | --- |
| `GET /health` | 无需登录 | 不含用户数据的存活检查 |
| `GET /devices` | 账清用户 JWT，向 Supabase Auth 核验 | 查询自己的设备 |
| `POST /devices` | 同上 | 创建设备，明文令牌仅返回一次 |
| `POST /devices/:id/rotate` | 同上并核对设备归属 | 换令牌、保留来源身份 |
| `POST /devices/:id/revoke` | 同上并核对设备归属 | 停止来源上传 |
| `POST /v1/bills` | 独立 Bearer 上传令牌 | 单张账单写入接收箱 |

接口基址为 Supabase 项目地址加 `/functions/v1/autoaccounting`。虽然平台 JWT 检查对该函数关闭，实际 handler 对所有敏感接口仍逐项鉴权；不能省略它的身份校验。

上传令牌使用 256 位随机值，服务器只保存 SHA-256。它不能读取账本、取代登录或操作支付。匿名用户不能读取接收箱；已登录用户只能读取自己的记录；客户端不能直接写新表或调用服务端专用 RPC。服务器密钥仅在函数环境中，不进入网页或手机。

不保存账户名称、截图、原始通知/OCR内容和支付密码。仅保留日期、金额、商户、商品说明、来源、分类和核对状态。响应禁止缓存；日志不输出请求头、令牌或账单内容。

去重键为「来源安装实例 + AutoAccounting 本地账单 ID」，并非支付平台交易号。业务内容哈希用于识别修订。同一内容重复上传返回成功但不更改版本时间。接收箱按 `updated_at,id` 游标每页 500 条读取；前端忽略记录和删除标记参与原有云同步合并。

错误统一返回 `{ok:false,code,message,requestId}`：401 验证失败、403 来源不允许、400/415 输入错误、413 内容过大、409 容量限制、429 频率限制、5xx 服务异常。合法但需核对的账单仍成功接收并返回 2xx。

限制：每用户最多 20 个有效设备、50,000 条接收记录；每设备每分钟 1,000 次上传；单条请求不超过 256 KB。未来扩容须显式调整，不会自动删除旧账。

## 部署维护

数据库迁移：`supabase/migrations/202609120001_autoaccounting.sql`，只新增表/函数及其权限。

函数：`supabase/functions/autoaccounting/`，使用 Supabase 提供的服务端环境变量。部署单个 `autoaccounting` 函数，不使用 prune，不重置数据库。

发布前检查：输入归一化、重放/修订、跨账号隔离、撤销令牌、手机布局、旧账本/PWA兼容。真实接口验证使用临时测试用户并在结束后删除，不往真实账本注入测试支出。

参考：[AutoAccounting 官方发布](https://github.com/AutoAccountingOrg/AutoAccounting/releases/tag/4.0.2-Canary.20260904_1534)、[Webhook 导出实现](https://github.com/AutoAccountingOrg/AutoAccounting/pull/1390)、[Supabase 函数验证说明](https://supabase.com/docs/guides/functions/auth)。
